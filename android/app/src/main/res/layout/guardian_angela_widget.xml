<?xml version="1.0" encoding="utf-8"?>
<!--
    Home-screen widget layout for Guardian Angela.

    Two text rows (mode name + status) above a row of two action buttons
    (Arm and Quick Exit). All dimensions use dp so the system can scale
    the widget across different densities. RTL-safe: paddingStart/End and
    layout_marginStart are used throughout.
-->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@android:color/background_dark"
    android:orientation="vertical"
    android:paddingStart="16dp"
    android:paddingTop="12dp"
    android:paddingEnd="16dp"
    android:paddingBottom="12dp">

    <TextView
        android:id="@+id/widget_mode_name"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:ellipsize="end"
        android:maxLines="1"
        android:text="Guardian Angela"
        android:textAppearance="?android:attr/textAppearanceMedium"
        android:textColor="?android:attr/textColorPrimary" />

    <TextView
        android:id="@+id/widget_status"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="2dp"
        android:ellipsize="end"
        android:maxLines="1"
        android:text="Idle"
        android:textAppearance="?android:attr/textAppearanceSmall"
        android:textColor="?android:attr/textColorSecondary" />

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="8dp"
        android:orientation="horizontal">

        <Button
            android:id="@+id/widget_arm"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:drawableStart="@android:drawable/ic_input_add"
            android:drawablePadding="4dp"
            android:text="Arm"
            android:textAllCaps="false"
            android:textColor="?android:attr/textColorPrimary" />

        <Button
            android:id="@+id/widget_quick_exit"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginStart="8dp"
            android:layout_weight="1"
            android:drawableStart="@android:drawable/ic_menu_close_clear_cancel"
            android:drawablePadding="4dp"
            android:text="Exit"
            android:textAllCaps="false"
            android:textColor="?android:attr/textColorPrimary" />

    </LinearLayout>

</LinearLayout>
