<?xml version="1.0" encoding="utf-8"?>
<!--
  AppWidget provider metadata for GuardianAngelaAppWidget.
  minWidth/minHeight = 2 cells × 1 cell (110dp × 40dp per cell guideline).
  updatePeriodMillis = 0: updates are triggered programmatically by Dart via
  HomeWidget.updateWidget — no periodic OS-driven refresh needed.
  resizeMode = horizontal|vertical so users can adjust the widget.
  widgetCategory = home_screen (standard launcher widget).
-->
<appwidget-provider xmlns:android="http://schemas.android.com/apk/res/android"
    android:initialLayout="@layout/guardian_angela_app_widget"
    android:minWidth="220dp"
    android:minHeight="110dp"
    android:resizeMode="horizontal|vertical"
    android:updatePeriodMillis="0"
    android:widgetCategory="home_screen"
    android:description="@string/widget_description" />
