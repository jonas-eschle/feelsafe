/// Physical device button used as a hardware trigger.
///
/// See spec 03 §HardwareButtonDistressTrigger and §HardwareButtonConfig.
enum ButtonType {
  /// The volume-up button on the device.
  volumeUp,

  /// The volume-down button on the device.
  volumeDown,
}
