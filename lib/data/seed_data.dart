/// Seed-data entry point. Phase 6 fills in the two built-in modes
/// (Walk Mode + Date Mode), 8 disguised-reminder templates, and
/// per-step-type event defaults. Until then this is a stub that
/// throws [UnimplementedError].
library;

/// Idempotent first-launch seeding. Populates defaults for modes,
/// templates, distress chains, and per-step event defaults.
Future<void> seedData() async =>
    throw UnimplementedError('TODO: Phase 6 fills this in');
