# ─────────────────────────────────────────────────────────────────────────────
# coverage_excludes.txt — the coverage-of-LOGIC exclusion list (SINGLE SOURCE OF
# TRUTH, read by tool/coverage/filter_coverage.sh).
#
# Each non-comment line is ONE `lcov --remove` glob, matched against the lcov
# `SF:` source paths (repo-relative). The GA coverage target is "~99% of the
# LOGIC surface" (owner M5 mandate + CLAUDE.md D6), so the denominator must be
# human-authored, host-testable logic.
#
# HONESTY CONTRACT (non-negotiable): a glob may ONLY exclude code that has NO
# host-testable logic — generated code, or a platform passthrough whose only
# host-reachable statements are a platform-guard early-return + a MethodChannel/
# static-plugin call that throws MissingPluginException off-device. A file that
# contains parsing / state / fallback / formatting decisions stays in the
# denominator EVEN IF it is currently 0% covered — that is a C6/C7 coverage gap
# to CLOSE, never something to hide behind an exclusion.
#
# Measured against `flutter test --coverage` on 2026-06-09 (4068 host tests):
# raw total = 26705 instrumented lines, of which 50.2% (13396) are generated.
# ─────────────────────────────────────────────────────────────────────────────

# ── Generated code ───────────────────────────────────────────────────────────
# Not human-authored; mirrors analysis_options.yaml's analyzer `exclude:` block
# exactly (the same files the analyzer already treats as generated).

# flutter gen-l10n output — 14 AppLocalizations*.dart files (~770 generated
# getters per locale, 11.9-95.6% incidental coverage). Emitted into the arb-dir
# (l10n.yaml has no separate output-dir). Pure generated message lookups.
lib/l10n/*

# Drift adapters — 7 *.g.dart files (database.g.dart alone = 2499 lines of
# generated table/companion/query plumbing). Regenerated by build_runner; never
# hand-edited (CI build-runner job hard-fails on a stale diff).
*.g.dart

# freezed unions (none exist today, but kept to match the analyzer exclude and
# stay correct if a *.freezed.dart is ever generated). `--ignore-errors unused`
# in filter_coverage.sh tolerates a glob that matches nothing.
*.freezed.dart

# ── Platform passthroughs with NO host-testable logic ────────────────────────
# Each below was line-by-line verified to contain only a platform-guard early
# return plus a MethodChannel/static-plugin call that cannot execute on the
# (Linux) host. Their on-device behaviour is covered by the device-e2e suite
# (tool/device_e2e/*.sh on emulator-5554) and/or CI build-ios — NOT by host
# unit tests, which is why they are out of the HOST coverage-of-logic floor.

# system_ui_service.dart (16 lines) — both methods are `if (!Platform.isAndroid)
# { log; return; }  try { await channel.invokeMethod(...); } catch { log; }`.
# On the host Platform.isAndroid is false → only the iOS no-op return is
# reachable; the invokeMethod lines are pure Android channel plumbing. The
# per-preset launcher-alias swap is device-proven (stealth_icon_switch_test.dart).
lib/services/system_ui_service.dart

# wakelock_service.dart (13 lines) — thin `if (_isEnabled) return;
# await WakelockPlus.enable()/disable()` idempotency wrapper around a static
# plugin that throws MissingPluginException off-device. No host-reachable
# decision logic of substance.
lib/services/wakelock_service.dart

# ─────────────────────────────────────────────────────────────────────────────
# DELIBERATELY NOT EXCLUDED (host-testable logic — left in the denominator and
# now COVERED by M5 C6, NOT hidden). Recorded here so a future reader sees the
# decision was conscious. The five 0% safety services are driven by real host
# tests that exercise the production parse/detection/timing logic via
# TestDefaultBinaryMessenger mock channels + fakeAsync (NOT the simulations):
#
#   lib/services/contact_service.dart          → 100% (contact_service_real_test).
#   lib/services/call_state_service.dart       → 100% (call_state_service_real_test;
#     drives the real EventChannel _onNativeEvent + _parseCallState switch).
#   lib/services/flash_service.dart            → 100% (flash_service_real_test;
#     real SOS/continuous loops under fakeAsync; surfaced + fixed a stopFlash
#     Completer-hang production bug).
#   lib/services/location_service.dart         → 100% (location_service_real_test;
#     mocked geolocator channel — fallback decision, stale-note ISO formatting,
#     1000-point eviction).
#   lib/services/hardware_button_service.dart  → 81.6% (hardware_button_service_real_test;
#     all Android/pure-Dart distress-trigger logic covered via a @visibleForTesting
#     channel-subscription seam. The ~21 uncovered lines are the iOS headphone-remote
#     path — _startIos/_onIosButtonPress/_GuardianAudioHandler — which need a real
#     iOS audio session and are CI-build-ios / device-only, NOT host-reachable).
#   lib/main.dart                              → 92.3% (main_bootstrap_test;
#     bootstrap branches + GuardianAngelaApp theme/locale + recovery flow). The
#     remaining lines are main()'s real runApp entry + a few recovery edge taps.
#
# Pure-abstract protocol files (lib/services/protocols/*.dart) need no exclusion:
# 23 of 25 contribute zero instrumented lines (abstract declarations have no
# executable lines); the 2 that appear (location_service_protocol.dart,
# device_info_service_protocol.dart) hold concrete, well-covered data classes.
# ─────────────────────────────────────────────────────────────────────────────
