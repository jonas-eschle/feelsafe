# ─────────────────────────────────────────────────────────────────────────────
# coverage_floor.txt — the committed coverage-of-LOGIC ratchet floor (percent).
#
# The single number below is the MINIMUM acceptable line coverage of the
# filtered (logic-only) lcov report. tool/coverage/check_floor.sh reads it and
# FAILS CI if coverage-of-logic drops beneath it.
#
# RATCHET — only ever goes UP:
#   * Set to the CURRENT measured baseline, never the final target.
#   * M5 C5 baseline (2026-06-09) = 84.49%-of-logic (11220 / 13280 lines).
#   * M5 C6 ratchet (2026-06-09) = 87.14%-of-logic (11576 / 13284 lines)
#     after adding host tests for the 5 zero-coverage safety services
#     (hardware_button / location / flash / call_state / contact — the latter
#     four now 100%) + main.dart (76.9%→92.3%). +356 covered lines.
#   * M5 C6b ratchet (2026-06-09) = 89.46%-of-logic (11886 / 13286 lines)
#     after covering the rest of the NON-feature surface: recording (→100%),
#     phone (→100%), timing_slider (→100%), the feedback-history dao+repo+table
#     (0%→100%), contacts_repo / contacts_dao bulkUpdate+deleteAll / sms_retry
#     hashCode / database.open() / json_singleton default-resolveDir (→~100%),
#     audio (55→96%), messaging (82→92.5%), session_start_validator (74→94%),
#     service_providers wiring (60→95%), notification host branches (46→76%),
#     home_widget (→100%), permission_audit (→99%), chain_event /
#     permission_revocation toString (→100%), session_engine edges (97→99%),
#     pin_keypad / deceptive-dialog / logo tails. +310 covered lines. Found +
#     fixed a real _launchUrl/_dial bug (un-awaited return-in-try swallowed no
#     async rejection → an unhandled exception on a failed SMS/phone/url launch;
#     now `await`ed so the degrade-to-false catch fires). The remaining
#     non-feature gaps are device-only (Platform.is*-gated native paths in
#     notification/device_info/messaging/hardware_button/background_session ≈
#     96 lines), codegen-only Drift table DSLs (≈71 lines, throw at runtime),
#     and the thin Sentry SDK passthrough (≈9 lines) — left in the denominator.
#   * M5 C7a ratchet (2026-06-09) = 90.86%-of-logic (12077 / 13292 lines)
#     after covering the session/fake-call feature tail: session_controller
#     (80→2 missed), session_screen (70→2), end_session_overlay (14→0),
#     session_elapsed_clock (8→0), fake_call_screen (12→0),
#     fake_call_controller (4→0). +191 covered lines. Found + fixed a real
#     teardown bug (_stopForegroundService read `ref` during onDispose —
#     threw, was swallowed, persistent notification leaked; service instance
#     now captured at session start). The 4 remaining lines in these files
#     are defensive-unreachable (null-state timer guards, a dead stealth
#     ternary branch — C9 candidate, and an EngineRunning(-1) pattern arm).
#   * Floor set to 90.2 — ~0.6% under the measurement to absorb run-to-run
#     widget-test line jitter so the everyday gate never falsely reds. The
#     gate PASSES at the baseline today.
#
# TARGET = ~99%-of-logic (owner M5 mandate + CLAUDE.md "ratchets to 99% by
# Phase 9 (D6)"). C7 (the lib/features/ tail) raises actual coverage toward it
# and BUMPS this number as its tests land; C10 sets the final 99% target. Do
# NOT set this to 99 before the covering tests exist — that would red the
# everyday CI with no code change.
# ─────────────────────────────────────────────────────────────────────────────
90.2
