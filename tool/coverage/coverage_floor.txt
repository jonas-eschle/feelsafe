# ─────────────────────────────────────────────────────────────────────────────
# coverage_floor.txt — the committed coverage-of-LOGIC ratchet floor (percent).
#
# The single number below is the MINIMUM acceptable line coverage of the
# filtered (logic-only) lcov report. tool/coverage/check_floor.sh reads it and
# FAILS CI if coverage-of-logic drops beneath it.
#
# RATCHET — only ever goes UP:
#   * Set to the CURRENT measured baseline, never the final target.
#   * M5 C5 baseline (2026-06-09) = 84.49%-of-logic (11220 / 13280 lines).
#   * M5 C6 ratchet (2026-06-09) = 87.14%-of-logic (11576 / 13284 lines)
#     after adding host tests for the 5 zero-coverage safety services
#     (hardware_button / location / flash / call_state / contact — the latter
#     four now 100%) + main.dart (76.9%→92.3%). +356 covered lines.
#   * M5 C6b ratchet (2026-06-09) = 89.46%-of-logic (11886 / 13286 lines)
#     after covering the rest of the NON-feature surface: recording (→100%),
#     phone (→100%), timing_slider (→100%), the feedback-history dao+repo+table
#     (0%→100%), contacts_repo / contacts_dao bulkUpdate+deleteAll / sms_retry
#     hashCode / database.open() / json_singleton default-resolveDir (→~100%),
#     audio (55→96%), messaging (82→92.5%), session_start_validator (74→94%),
#     service_providers wiring (60→95%), notification host branches (46→76%),
#     home_widget (→100%), permission_audit (→99%), chain_event /
#     permission_revocation toString (→100%), session_engine edges (97→99%),
#     pin_keypad / deceptive-dialog / logo tails. +310 covered lines. Found +
#     fixed a real _launchUrl/_dial bug (un-awaited return-in-try swallowed no
#     async rejection → an unhandled exception on a failed SMS/phone/url launch;
#     now `await`ed so the degrade-to-false catch fires). The remaining
#     non-feature gaps are device-only (Platform.is*-gated native paths in
#     notification/device_info/messaging/hardware_button/background_session ≈
#     96 lines), codegen-only Drift table DSLs (≈71 lines, throw at runtime),
#     and the thin Sentry SDK passthrough (≈9 lines) — left in the denominator.
#   * M5 C7a ratchet (2026-06-09) = 90.86%-of-logic (12077 / 13292 lines)
#     after covering the session/fake-call feature tail: session_controller
#     (80→2 missed), session_screen (70→2), end_session_overlay (14→0),
#     session_elapsed_clock (8→0), fake_call_screen (12→0),
#     fake_call_controller (4→0). +191 covered lines. Found + fixed a real
#     teardown bug (_stopForegroundService read `ref` during onDispose —
#     threw, was swallowed, persistent notification leaked; service instance
#     now captured at session start). The 4 remaining lines in these files
#     are defensive-unreachable (null-state timer guards, a dead stealth
#     ternary branch — C9 candidate, and an EngineRunning(-1) pattern arm).
#   * M5 C7b ratchet (2026-06-09) = 92.05%-of-logic (12245 / 13302 lines)
#     after covering the lib/features/modes/ surface — ALL 10 files now 0
#     missed: event_specific_config (51→0), safety_options_section (31→0),
#     modes_controller (25→0, real ProviderContainer + in-memory Drift),
#     mode_event_defaults (12→0, parametrised per-ChainStepType _replace
#     routing), stealth_config_fields (11→0), modes_screen (10→0),
#     step_config_panel (6→0), gps_logging_fields (5→0), sms_contact_grid
#     (4→0), config_fields (3→0). +168 covered lines. Found + fixed a real
#     clear-to-null bug (red→green proven): _PhoneCallContactForm and
#     _CallEmergencyForm cleared their nullable field via copyWith(null),
#     which `x ?? this.x`-keeps the old value — clearing a previously-set
#     emergency number / primary contact silently did nothing; both now use
#     the file's documented direct-construction pattern.
#   * M5 C7c ratchet (2026-06-09) = 93.11%-of-logic (12385 / 13302 lines)
#     after covering the editors + distress surface — 9 of 11 files now 0
#     missed: distress_modes_controller (42→0, real ProviderContainer +
#     in-memory Drift + recording settings repo; every delete guard pinned in
#     BOTH directions, two red→green-proven), reminder_templates_screen
#     (33→1, FAB add-sheet from-scratch/from-template/builtin-picker flows),
#     template_editor_screen (22→0, dirty-cancel discard guard + PopScope),
#     reminder_templates_controller (17→0), event_defaults_controller (10→0),
#     mode_editor_screen (6→2, unsaved Keep/Discard + manage-contacts),
#     reminder_template_form (5→0, icon dropdown + isDirty), distress_modes
#     _screen (4→1, in-use tooltip + defensive in-use-delete snackbar),
#     reminder_confirmation (3→0, FileImage/asset-ext/disguise-fallback),
#     event_defaults_screen (1→0, hardwareButton save slot),
#     mode_editor_controller (1→0, load-unknown StateError). +140 covered
#     lines. Also made the fake-call hold-to-distress test deterministic via
#     an injectable FakeCallScreen.now clock seam (was wall-clock flaky).
#     The 4 remaining lines in these files are 2 defensive-unreachable
#     _issueMessage arms (distressNoActionStep is only ever emitted
#     non-blocking; the arm exists for switch exhaustiveness — C9 candidate)
#     and 2 const-constructor lines that flip with const-canonicalization
#     jitter (DistressModesScreen/ReminderTemplatesScreen `super.key` lines).
#   * M5 C7d ratchet (2026-06-09) = 94.47%-of-logic (12566 / 13302 lines)
#     after covering the session-log history/trash/retention surface — ALL
#     6 files now 0 missed: past_events_trash_screen (41→0, overflow-menu →
#     typed "EMPTY TRASH" confirm dialog → emptyTrash + SnackBar),
#     past_events_trash_controller (39→0, real ProviderContainer + in-memory
#     Drift + recording settings repo; on-open purge honours
#     trashRetentionDays BOTH ways, restore un-trashes so a later purge
#     spares it while its still-trashed sibling dies, emptyTrash kills
#     critical trash but spares live rows, purge-failure non-fatal),
#     past_events_detail_screen (38→0, sharePdf via mocked net.nfet.printing
#     channel asserting real %PDF bytes + log-id filename, delete-confirm →
#     SOFT delete + pop), past_events_controller (36→0, outcomeFromEndReason
#     all 8 arms + soft/undo/hard delete: list delete NEVER destroys a
#     CRITICAL log — red→green proven against a deleteById mutation),
#     history_retention_controller (15→0, setters persist without clobbering
#     the sibling window), history_retention_screen (11→0, Purge-now drives
#     the real repo purge end-to-end → count SnackBar). +181 covered lines.
#     No production bugs found in this surface. The trash-screen super.key
#     const line is pinned with a non-const UniqueKey() construction
#     (C7b's de-jitter trick).
#   * Floor set to 93.8 — ~0.6% under the measurement to absorb run-to-run
#     widget-test line jitter so the everyday gate never falsely reds. The
#     gate PASSES at the baseline today.
#
# TARGET = ~99%-of-logic (owner M5 mandate + CLAUDE.md "ratchets to 99% by
# Phase 9 (D6)"). C7 (the lib/features/ tail) raises actual coverage toward it
# and BUMPS this number as its tests land; C10 sets the final 99% target. Do
# NOT set this to 99 before the covering tests exist — that would red the
# everyday CI with no code change.
# ─────────────────────────────────────────────────────────────────────────────
93.8
